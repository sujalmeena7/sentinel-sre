[pytest]
# Tests import backend modules directly (`import main`), so the backend dir
# must be on sys.path — conftest.py inserts it before `main` is imported.
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:slowapi.*

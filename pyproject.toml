[project]
name = "ai-root-cause-analyzer"
version = "0.1.0"
# The backend is deployed on 3.12 (see backend/runtime.txt) and developed on
# 3.10+. Dependencies live in backend/requirements.txt, not here.
requires-python = ">=3.10,<3.13"
dependencies = []
